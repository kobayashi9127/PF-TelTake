.container.mt-5
  .row
    .row.mt-5
    .col-12
      table.table
        thead
          tr
            th コメント
            th
            th 日付
            th
        tbody
          - @comments.each do |comment|
            tr
             td = comment.body
             td
             td = comment.created_at.strftime("%Y年%m月%d日")


      = paginate @comments
